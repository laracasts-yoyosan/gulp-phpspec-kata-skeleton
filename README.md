# Introduction

Some intro.

# Usage

## Prerequisite

Install gulp globally if you don't have it.

```
sudo npm install -g gulp
```

```
composer install
npm install
./vendor/bin/phpspec run
# or
gulp
```
